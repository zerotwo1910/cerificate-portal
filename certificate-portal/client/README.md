# Certificate Portal — Client (Angular 19.2.27)

Frontend for the Certificate Portal. See the top-level `README.md` and
`RUNBOOK.md` (one directory up) for full setup instructions, architecture
notes, and demo login credentials.

Quick start (after the backend is running):
```bash
npm install
npm start   # ng serve → http://localhost:4200
```
